# Install to /etc/mv-telemetry.env as root:root, chmod 600. NOT stored in git.
# Generate the secret with:  python3 -c 'import secrets; print(secrets.token_urlsafe(64))'
MV_SECRET_KEY=__SECRET__
MV_DB_PATH=/var/lib/mv-telemetry/db.sqlite3
# Never set MV_DEBUG in production.
